type Query {
  songsByName(name: String): [Song!]!
  songsByIds(ids: [String!]!): [Song!]!
  queue: [QueueItem!]!
  artistsByName(name: String): [Artist!]!
  artistById(id: String!): Artist!
  streamingUrl(id: String!): String!
  scoringData(id: String!): [Int!]!
}

type Mutation {
  queueSong(songId: String!): Boolean!
  popSong: QueueItem
  removeSong(songId: String!, timestamp: String!): Boolean!
}

type Artist {
  id: ID!
  name: String!
  nameYomi: String!
  songCount: Int!
  songs: [Song!]!
}

type Song {
  id: ID!
  name: String!
  nameYomi: String!
  artistName: String!
  artistNameYomi: String!
  lyricsPreview: String
}

type QueueItem {
  songId: String!
  timestamp: String!
}
